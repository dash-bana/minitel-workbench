# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# App runtime state (never commit a user's settings/recordings)
*.vdt
/recordings/
site/

# macOS
.DS_Store

# The original uploaded transcript lives in the repo as docs/design/design-notebook.md
/Minitel Workbench.md

# Claude Code permission settings — personal / machine-specific, keep local.
# (Broad allow patterns like `ssh *` / `python3 *` should not ship in a shared repo.)
.claude/settings.json
.claude/settings.local.json

# Editors
.idea/
.vscode/
*.swp
*.mtr

